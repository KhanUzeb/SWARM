services:
  swarm:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - swarm-data:/app/data
    restart: unless-stopped

volumes:
  swarm-data:
