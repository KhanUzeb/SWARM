## Why

<!-- Product reason: what was wrong and what this changes. -->

## What changed

<!-- Backend / frontend / docs. Link issues with Fixes #N. -->

## How it was tested

<!-- e.g. `pytest -q` (N passed), `bun run build` success, manual smoke test. -->
<!-- Describe results in words. Never paste secrets, tokens, local paths, or machine names. -->

- [ ] `pytest -q` green
- [ ] `bun run build` green (if frontend touched)
- [ ] No secrets or personal data in the diff
