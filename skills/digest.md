When to use: catch up on a long thread without rereading it.

Inputs: this channel. Optional topic after the command (`/digest migration`).

Steps:
1. Use channel_digest (and search_channel_history if a topic is given).
2. Drop greetings and repeats. Keep decisions, links, and open questions.
3. Note who said the important bits.

Output: a tight summary (facts, then open questions). Write `digest.md` to the workspace if it is longer than a screen.

Approval: none.
